version: '3.8'

services:
  mailhog:
    image: mailhog/mailhog:latest
    container_name: mailhog-dev
    ports:
      - "1025:1025" # SMTP server (for Node.js)
      - "8025:8025" # Web UI (for your browser)
